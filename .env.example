# VDT Variance Analysis — see docs/adr/0034. Copy to .env and fill in a real
# key; without it, variance analysis fails in isolation (bridge/table still work).
OPENAI_API_KEY=
OPENAI_MODEL=gpt-4o-mini
