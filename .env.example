# VDT Statement movement narration — see docs/adr/0034. Copy to .env and fill
# in a real key; without it, narration fails in isolation (bridge/table still work).
OPENAI_API_KEY=
OPENAI_MODEL=gpt-4o-mini
